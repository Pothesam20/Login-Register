{
  "name": "login-register-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd login-register && npm start",
    "build": "cd login-register && npm build",
    "test": "cd login-register && npm test",
    "install-all": "npm install && cd login-register && npm install"
  },
  "dependencies": {
    "react-icons": "^5.5.0"
  }
}
